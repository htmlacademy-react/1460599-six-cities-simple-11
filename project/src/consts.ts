export const RATING_MULTIIER_FOR_STYLES = 20;

export enum AppRoute {
  Root = '/',
  Login = '/login',
  Room = '/offer',
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
